<UserControl x:Class="SnowblindModPlayer.Views.MonitorSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Border Padding="0">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Select Monitor for Fullscreen Display"
                       FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource Brush.TextMuted}"/>

            <!-- Monitor Layout Canvas -->
            <Border BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" Padding="12" Background="{DynamicResource Brush.Panel2}" CornerRadius="8" Margin="0,0,0,16">
                <Canvas x:Name="MonitorCanvas" Height="260" Background="Transparent"/>
            </Border>

            <!-- Selected Monitor Info -->
            <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                <TextBlock Text="Selected Monitor:" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <Border Background="{DynamicResource Brush.Panel}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" Padding="12" CornerRadius="8">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding SelectedMonitor.DisplayName, FallbackValue='No monitor selected'}"
                                   FontSize="14" FontWeight="SemiBold"/>
                        <TextBlock Margin="5,5,0,0">
                            <Run Text="Position: "/><Run Text="{Binding SelectedMonitor.X, FallbackValue='-'}"/><Run Text=", "/><Run Text="{Binding SelectedMonitor.Y, FallbackValue='-'}"/>
                        </TextBlock>
                        <TextBlock Margin="5,2,0,0">
                            <Run Text="Resolution: "/><Run Text="{Binding SelectedMonitor.Width, FallbackValue='-'}"/><Run Text="x"/><Run Text="{Binding SelectedMonitor.Height, FallbackValue='-'}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>

            <TextBlock Text="Available Monitors:" FontWeight="SemiBold" Margin="0,16,0,10"/>

            <!-- Monitor List -->
            <ItemsControl ItemsSource="{Binding AvailableMonitors}" HorizontalAlignment="Left">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Margin="0,0,10,10" Padding="14,8"
                                ToolTip="{Binding DisplayName}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding DisplayName}"/>
                                <TextBlock Text=" (" Margin="5,0,0,0"/>
                                <TextBlock Text="{Binding Width}"/><TextBlock Text="x"/>
                                <TextBlock Text="{Binding Height}"/><TextBlock Text=")"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
